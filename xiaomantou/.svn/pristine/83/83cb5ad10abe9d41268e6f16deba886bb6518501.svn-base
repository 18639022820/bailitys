<?php

/**
 * 路径转换
 */
function attach($path,$value){

	return 'data/upload/' . $path . '/' . $value;

}
/**
 * object转换为数组
 */
function objectToArray($object) {
    //先编码成json字符串，再解码成数组
    return json_decode(json_encode($object), true);
}

